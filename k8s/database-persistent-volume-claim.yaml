 apiVersion: v1
 kind: PersistentVolumeClaim
 metadata:
   name: database-persistent-volume-claim
 spec:
   accessModes:
     - ReadWriteOnce
   storageClassName: notejam-storageclass
   resources:
     requests:
       storage: 2Gi
